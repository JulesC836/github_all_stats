services:
  - type: web
    name: my-app
    runtime: node
    buildCommand: npm install
    startCommand: node index.js
    routes:
      - type: rewrite
        source: /*
        destination: /index.js